/* const form = document.querySelector('.forms-div form')
// console.log(form)
if (form.children) {
  const array = [...form.childNodes]

  const btn = form.childNodes[form.childNodes.length - 1]
  const inputs = array.filter((child)=> child !== btn)
}

const firstForm = document.querySelector(".first-form")
const secondForm = document.querySelector(".second-form")
const passwordForm = document.querySelector(".password-form")
const lastForm = document.querySelector(".last-form") */

/* if(firstForm){
  form.addEventListener("submit", (event)=>{
    event.preventDefault()
    // console.log("First form")
  })
}

if(secondForm){
  form.addEventListener("submit", (event)=>{
    event.preventDefault()
    // console.log("Second form")
  })
}

if(passwordForm){
  form.addEventListener("submit", (event)=>{
    event.preventDefault()
    // console.log("Password form")
  })
}

if(lastForm){
  form.addEventListener("submit", (event)=>{
    event.preventDefault()
    // console.log("Last form")
  })
} */
"use strict";